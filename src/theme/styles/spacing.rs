/// Spacing scale constants for padding, margin, and gap values.
/// Based on a 4px base unit. Use these instead of bare pixel literals.

/// 0px — Zero spacing (explicit reset)
pub const SPACE_0: f32 = 0.0;

/// 2px — Hairline spacing (tight list items, minimal separation)
pub const SPACE_0_5: f32 = 2.0;

/// 4px — Extra-small spacing (compact padding, divider margins, icon gaps)
pub const SPACE_1: f32 = 4.0;

/// 6px — Small-tight spacing (compact button padding-y, divider margins)
pub const SPACE_1_5: f32 = 6.0;

/// 8px — Small spacing (section padding, small button padding-x)
pub const SPACE_2: f32 = 8.0;

/// 10px — Base spacing (card padding, content padding, standard row gap)
pub const SPACE_2_5: f32 = 10.0;

/// 12px — Base-plus spacing (column gap between status items)
pub const SPACE_3: f32 = 12.0;

/// 16px — Medium spacing (card padding, button horizontal padding)
pub const SPACE_4: f32 = 16.0;

/// 20px — Large spacing (content area padding, section row gap)
pub const SPACE_5: f32 = 20.0;

/// 24px — Extra-large spacing (panel top padding)
pub const SPACE_6: f32 = 24.0;

/// 32px — 2XL spacing (decorative separator margin)
pub const SPACE_8: f32 = 32.0;

/// 40px — 3XL spacing (large section gap, title menu bottom padding)
pub const SPACE_10: f32 = 40.0;

/// 64px — 4XL spacing (reserved for very large outer spacing)
pub const SPACE_16: f32 = 64.0;

/// 96px — 5XL spacing (title screen horizontal padding)
pub const SPACE_24: f32 = 96.0;
